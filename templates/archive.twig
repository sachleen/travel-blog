{% if posts|length > 0 %}
<ul>
{% for post in posts %}
    <li>{{ post.metadata.timestamp|date('d/m/Y') }} - <a href="pages/{{ post.metadata.slug }}">{{ post.metadata.title }}</a></li>
{% endfor %}
</ul>
{% endif %}


